# Prototype Data

This folder contains static seed data for the first Tide Planner web prototype.

The current location records and tide profiles are not the final source of truth. Production location, dataset, verification, and admin metadata should move to:

```text
V0_Tide_Planner
```

Configured Supabase project ref:

```text
iztlyyavbdgfqfymqwzz
```

Important boundaries:

- Kenya placeholder locations use the Mombasa/Kenya coast reference profile until locally calibrated.
- Source and verification labels must remain visible in the UI.
- Do not mark static prototype data as verified unless local verification evidence is recorded.
- Do not connect this app to the Seaweed Station v4 Supabase database for Tide Planner source-of-truth data.
